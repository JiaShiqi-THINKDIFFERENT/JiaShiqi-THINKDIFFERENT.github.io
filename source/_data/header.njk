{#
  顶部导航条（P0-1）
  - 经 _config.next.yml 的 custom_file_path.header 注入到 header 内部末尾。
  - 本条只承载两个元素（自左向右）：
      1. 侧栏折叠按钮 —— 控制左侧菜单栏收起 / 展开，状态由 sidebar-toggle.js 维护；
      2. 品牌字标 CLIVIA —— 点击回首頁。
    站点导航项（首页 / 个股研究 / 三好评分 / 分类 / 标签 / 归档 / 关于）
    保持在侧栏菜单（主题 menu 配置），此处不重复放置。
  - 移动端(<992px)由 CSS 隐藏本条，沿用 NexT 自带的品牌横幅与汉堡菜单。
  - 折叠按钮的事件绑定、箭头翻转、localStorage 持久化均在 sidebar-toggle.js，
    这里只输出静态结构；模板中已带 aria-controls / label，无 JS 时也不报错。
#}
<nav class="clivia-navbar" aria-label="站点顶部栏">
  <div class="clivia-navbar-inner">
    <button class="clivia-sidebar-toggle" type="button"
            aria-controls="clivia-sidebar" aria-label="折叠侧边栏" title="折叠侧边栏">
      <svg viewBox="0 0 24 24" width="15" height="15" fill="none"
           stroke="currentColor" stroke-width="2.4" stroke-linecap="round"
           stroke-linejoin="round" aria-hidden="true"><polyline points="15 5 8 12 15 19"></polyline></svg>
    </button>
    <a class="clivia-navbar-brand" href="/">CLIVIA</a>
  </div>
</nav>
